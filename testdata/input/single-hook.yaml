apiVersion: batch/v1
kind: Job
metadata:
  name: myapp-init
  annotations:
    helm.sh/hook: pre-install
    helm.sh/hook-weight: "5"
spec:
  template:
    spec:
      containers:
        - name: init
          image: busybox
          command: ["echo", "init"]
      restartPolicy: Never
